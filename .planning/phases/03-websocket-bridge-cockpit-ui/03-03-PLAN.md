---
phase: 03-websocket-bridge-cockpit-ui
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - ui/src/components/ElevationProfile.tsx
  - ui/src/components/MiniMap.tsx
  - ui/src/App.tsx
autonomous: false
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "Elevation profile renders as a 120px strip at bottom of cockpit"
    - "Elevation profile shows empty-state label 'Keine Strecke geladen'"
    - "Mini-map renders as 160x160px tile in top-right corner"
    - "Mini-map shows empty-state label 'Keine Strecke'"
    - "Mini-map loads CartoDB dark matter tiles"
    - "Full cockpit is visually correct: dark theme, speed primary, gear prominent"
  artifacts:
    - path: "ui/src/components/ElevationProfile.tsx"
      provides: "Recharts AreaChart empty-state scaffold"
      exports: ["ElevationProfile"]
    - path: "ui/src/components/MiniMap.tsx"
      provides: "Leaflet mini-map empty-state scaffold"
      exports: ["MiniMap"]
    - path: "ui/src/App.tsx"
      provides: "Full cockpit layout with all components wired"
      contains: "ElevationProfile"
  key_links:
    - from: "ui/src/App.tsx"
      to: "ui/src/components/ElevationProfile.tsx"
      via: "component import + render in bottom strip"
      pattern: "ElevationProfile"
    - from: "ui/src/App.tsx"
      to: "ui/src/components/MiniMap.tsx"
      via: "component import + render in top-right"
      pattern: "MiniMap"
---

<objective>
Add the empty-state ElevationProfile (Recharts) and MiniMap (Leaflet) scaffold components to the cockpit, then visually verify the complete cockpit layout.

Purpose: UI-02 (elevation profile empty-state) + UI-03 (mini-map empty-state) + full visual checkpoint.
Output: Complete cockpit with all 7 components rendered; human-verified.
</objective>

<execution_context>
@/Users/partydj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/partydj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-websocket-bridge-cockpit-ui/03-RESEARCH.md
@.planning/phases/03-websocket-bridge-cockpit-ui/03-UI-SPEC.md
@.planning/phases/03-websocket-bridge-cockpit-ui/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ElevationProfile and MiniMap empty-state components, wire into App</name>
  <files>
    ui/src/components/ElevationProfile.tsx,
    ui/src/components/MiniMap.tsx,
    ui/src/App.tsx
  </files>
  <read_first>
    .planning/phases/03-websocket-bridge-cockpit-ui/03-UI-SPEC.md,
    .planning/phases/03-websocket-bridge-cockpit-ui/03-RESEARCH.md,
    ui/src/App.tsx,
    ui/src/types/telemetry.ts,
    ui/src/hooks/useTelemetry.ts
  </read_first>
  <action>
    **1. Create ui/src/components/ElevationProfile.tsx:**
    ```tsx
    import { memo } from "react";
    import { AreaChart, Area, ResponsiveContainer } from "recharts";

    const EMPTY_DATA = [{ x: 0, y: 0 }, { x: 1, y: 0 }];

    export const ElevationProfile = memo(function ElevationProfile() {
      return (
        <div className="relative w-full h-full bg-[#111111]">
          <ResponsiveContainer width="100%" height="100%">
            <AreaChart data={EMPTY_DATA}>
              <Area
                type="linear"
                dataKey="y"
                stroke="#374151"
                fill="#374151"
                dot={false}
                isAnimationActive={false}
              />
            </AreaChart>
          </ResponsiveContainer>
          <span className="absolute inset-0 flex items-center justify-center text-xs text-[#6B7280]">
            Keine Strecke geladen
          </span>
        </div>
      );
    });
    ```
    - Recharts AreaChart with flat empty data. Gray-700 stroke and fill.
    - Overlay text "Keine Strecke geladen" centered, 12px, gray-500.
    - `isAnimationActive={false}` — no animation on mount.
    - Background: #111111 (secondary color from UI-SPEC).
    - No axes, no grid — only the area path visible.

    **2. Create ui/src/components/MiniMap.tsx:**
    ```tsx
    import { memo } from "react";
    import { MapContainer, TileLayer } from "react-leaflet";
    import "leaflet/dist/leaflet.css";

    const CARTO_DARK = "https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png";
    const ATTRIBUTION = '&copy; <a href="https://www.openstreetmap.org/copyright">OSM</a> &copy; <a href="https://carto.com/">CARTO</a>';

    // Default center: roughly central Europe (doesn't matter for empty state)
    const DEFAULT_CENTER: [number, number] = [50.0, 10.0];
    const DEFAULT_ZOOM = 5;

    export const MiniMap = memo(function MiniMap() {
      return (
        <div className="relative w-[160px] h-[160px] rounded-lg overflow-hidden bg-[#111111] shrink-0">
          <MapContainer
            center={DEFAULT_CENTER}
            zoom={DEFAULT_ZOOM}
            className="w-full h-full"
            zoomControl={false}
            attributionControl={false}
            dragging={false}
            scrollWheelZoom={false}
            doubleClickZoom={false}
            touchZoom={false}
          >
            <TileLayer url={CARTO_DARK} attribution={ATTRIBUTION} />
          </MapContainer>
          <span className="absolute inset-0 flex items-center justify-center text-xs text-[#6B7280] pointer-events-none z-[1000]">
            Keine Strecke
          </span>
        </div>
      );
    });
    ```
    - Import `leaflet/dist/leaflet.css` explicitly — Pitfall 4.
    - 160x160px, rounded-lg (8px), no border.
    - CartoDB dark matter tiles (no API token needed).
    - All map interactions disabled (dragging, zoom, scroll) — display only.
    - Overlay text "Keine Strecke" centered, 12px, gray-500, pointer-events-none so it doesn't block map.
    - z-[1000] so overlay text renders above Leaflet tile layers.

    **3. Update ui/src/App.tsx** to include ElevationProfile and MiniMap:
    - Add imports for ElevationProfile and MiniMap
    - Change layout from simple flex-col to grid with mini-map in top-right:
    ```tsx
    import { useTelemetry } from "./hooks/useTelemetry";
    import { ConnectionBanner } from "./components/ConnectionBanner";
    import { MetricDisplay } from "./components/MetricDisplay";
    import { GearStrip } from "./components/GearStrip";
    import { GradeBar } from "./components/GradeBar";
    import { ElevationProfile } from "./components/ElevationProfile";
    import { MiniMap } from "./components/MiniMap";

    function App() {
      const { telemetry: t, status } = useTelemetry();

      return (
        <div className="w-screen h-screen bg-black overflow-hidden flex flex-col">
          <ConnectionBanner status={status} />
          <div className="flex-1 grid grid-cols-[1fr_auto] p-6 gap-8 min-h-0">
            <div className="flex flex-col gap-8">
              <MetricDisplay
                value={t?.speed_kmh?.toFixed(1) ?? "\u2014"}
                unit="km/h"
                size="display"
              />
              <GearStrip gear={t?.gear ?? null} />
              <div className="flex gap-8">
                <MetricDisplay
                  value={t?.power_w ?? "\u2014"}
                  unit="Watt"
                  size="body"
                />
                <MetricDisplay
                  value={t?.cadence_rpm ?? "\u2014"}
                  unit="U/min"
                  size="body"
                />
              </div>
              <GradeBar
                real={t?.real_grade_pct ?? 0}
                effective={t?.effective_grade_pct ?? 0}
              />
            </div>
            <MiniMap />
          </div>
          <div className="h-[120px] shrink-0">
            <ElevationProfile />
          </div>
        </div>
      );
    }

    export default App;
    ```
    Key changes from Plan 02's App.tsx:
    - Added `grid grid-cols-[1fr_auto]` to the content area (was flex-col)
    - MiniMap rendered in the right column
    - Bottom 120px div now wraps ElevationProfile component instead of being an empty placeholder
  </action>
  <verify>
    <automated>cd /Users/partydj/Desktop/Projekte/RideOS/ui && npx tsc --noEmit && npm run build && echo "BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/components/ElevationProfile.tsx contains `export const ElevationProfile = memo(`
    - ui/src/components/ElevationProfile.tsx contains `Keine Strecke geladen`
    - ui/src/components/ElevationProfile.tsx contains `AreaChart`
    - ui/src/components/ElevationProfile.tsx contains `bg-[#111111]`
    - ui/src/components/MiniMap.tsx contains `export const MiniMap = memo(`
    - ui/src/components/MiniMap.tsx contains `Keine Strecke`
    - ui/src/components/MiniMap.tsx contains `leaflet/dist/leaflet.css`
    - ui/src/components/MiniMap.tsx contains `cartocdn.com/dark_all`
    - ui/src/components/MiniMap.tsx contains `w-[160px] h-[160px]`
    - ui/src/App.tsx contains `import { ElevationProfile }`
    - ui/src/App.tsx contains `import { MiniMap }`
    - ui/src/App.tsx contains `grid grid-cols-[1fr_auto]`
    - ui/src/App.tsx contains `<ElevationProfile />`
    - ui/src/App.tsx contains `<MiniMap />`
    - `cd ui && npx tsc --noEmit` exits 0
    - `cd ui && npm run build` exits 0
  </acceptance_criteria>
  <done>
    ElevationProfile renders empty Recharts AreaChart with "Keine Strecke geladen" overlay. MiniMap renders 160x160px Leaflet map with CartoDB dark tiles and "Keine Strecke" overlay. App.tsx has full cockpit grid layout with all 7 components. Production build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete cockpit</name>
  <what-built>
    Complete cockpit UI with all components:
    1. Speed (72px primary metric)
    2. Gear strip (10 gears, active gear blue pill)
    3. Watt + Cadence (secondary metrics)
    4. Grade bar (real vs effective with color coding)
    5. Connection banner (amber — engine not running)
    6. Elevation profile (120px bottom strip, empty state)
    7. Mini-map (160x160px top-right, dark tiles)
  </what-built>
  <how-to-verify>
    1. Start the UI dev server:
       ```bash
       cd /Users/partydj/Desktop/Projekte/RideOS/ui && npm run dev
       ```
    2. Open http://localhost:5173 in browser

    3. Verify WITHOUT engine running:
       - Amber connection banner visible at top: "Verbindung wird hergestellt..."
       - All metric values show em-dash placeholder
       - Gear strip shows gears 1-10 (gear 5 may show as default or null)
       - Dark black background, no scrolling
       - Elevation profile visible at bottom (120px, gray, "Keine Strecke geladen")
       - Mini-map visible top-right (160x160px, dark map tiles, "Keine Strecke")

    4. (Optional) Start engine to test live data:
       ```bash
       cd /Users/partydj/Desktop/Projekte/RideOS/engine && uv run python -m engine
       ```
       - Connection banner should disappear
       - Metrics should update with live values from KICKR

    5. Verify layout matches the UI-SPEC cockpit grid:
       - Speed top-left, large
       - Mini-map top-right
       - Gear + watt/cadence in middle
       - Grade bar below metrics
       - Elevation profile full-width bottom strip
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd /Users/partydj/Desktop/Projekte/RideOS/ui && npm run build` — production build succeeds
- `cd /Users/partydj/Desktop/Projekte/RideOS/engine && uv run pytest tests/ -ra` — full Python test suite green
- All 7 components render in browser without errors (console clean)
- Visual layout matches UI-SPEC cockpit grid
</verification>

<success_criteria>
- ElevationProfile renders empty-state Recharts chart with German label
- MiniMap renders CartoDB dark tiles at 160x160px with German label
- App.tsx uses grid layout with mini-map in top-right, elevation at bottom
- Full cockpit visually matches UI-SPEC layout specification
- Production build succeeds (Tailwind classes not purged)
- Human approves visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/03-websocket-bridge-cockpit-ui/03-03-SUMMARY.md`
</output>
