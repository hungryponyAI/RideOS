---
phase: 04-gpx-route-integration
plan: 05
type: execute
wave: 5
depends_on:
  - "04-04"
files_modified: []
autonomous: false
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
user_setup: []

must_haves:
  truths:
    - "End-to-end: user opens UI → loads real GPX via pre-ride screen → MiniMap fitBounds draws the route → pedals → amber CircleMarker advances → ElevationProfile ReferenceLine advances → KICKR resistance changes to match grade"
    - "BLE reconnect during ride does not crash position tracking (amber marker freezes, resumes)"
    - "Reaching the end of the route sets grade to 0% (trainer goes flat); rider can keep pedaling freely"
  artifacts: []
  key_links:
    - from: "ElevationProfile amber ReferenceLine"
      to: "telemetry.position_m"
      via: "React prop updated at WS tick rate"
      pattern: "positionM"
    - from: "KICKR resistance"
      to: "state.real_grade_percent (driven by RouteTracker)"
      via: "run_control_loop"
      pattern: "write_grade|control_point"
---

<objective>
Human-verify the full Phase 4 end-to-end loop on real hardware. All implementation (plans 04-01/02/03/04) is complete and unit-tested; this checkpoint confirms the behavior that unit tests cannot cover: actual KICKR Core resistance responding to GPX-driven grade, visual marker sync between ElevationProfile + MiniMap + telemetry ticks, and handling of BLE reconnect during a live ride.

Purpose: Phase 4 acceptance gate. No automated test can prove a physical trainer changes resistance matching a route's hills — this is the last mile.
Output: Signed checkpoint; any issues surface as gap-closure items for a 04-06 plan if needed.
</objective>

<execution_context>
@/Users/partydj/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-gpx-route-integration/04-CONTEXT.md
@.planning/phases/04-gpx-route-integration/04-04-SUMMARY.md
@.planning/phases/04-gpx-route-integration/04-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end ride verification with real KICKR Core + GPX</name>
  <files>(none — human verification checkpoint; no files modified)</files>
  <read_first>
    - .planning/phases/04-gpx-route-integration/04-CONTEXT.md §"Route Lifecycle"
    - .planning/phases/04-gpx-route-integration/04-VALIDATION.md §"Manual-Only Verifications"
    - ROADMAP.md §"Phase 4" success criteria
  </read_first>
  <what-built>
    Complete Phase 4 stack:
    - Engine: `engine.route.loader.load_gpx` + `engine.route.tracker.RouteTracker` + main.py wiring (DEFAULT_GRADE=0.0, RouteContext, extended telemetry snapshot)
    - WS: `{"type":"load_route"}` inbound, `{"type":"route_data"}` + `{"type":"route_error"}` + extended `{"type":"telemetry","position_m":...,"route_loaded":...}` outbound
    - UI: PreRideScreen with GPX picker; live ElevationProfile (red/blue gradient + amber ReferenceLine); live MiniMap (gray Polyline + amber CircleMarker); route data stored in useRef (not state) to avoid re-render thrash
  </what-built>
  <action>
    This is a human-verify checkpoint — no automated code changes. The human runs through the verification procedure in `<how-to-verify>` and either confirms all items pass or captures specific failures.

    Responsibilities for the executing Claude:
    1. Confirm the full suite is still green before surfacing the checkpoint: `cd /Users/partydj/Desktop/Projekte/RideOS/engine && uv run pytest -x -q`
    2. Confirm the UI builds: `cd /Users/partydj/Desktop/Projekte/RideOS/ui && npm run build`
    3. Surface the checkpoint to the user — print the verification checklist in a readable form and pause for the resume-signal.
    4. On "approved": mark plan 04-05 complete; update STATE.md to reflect Phase 4 completion; commit an empty documentation commit noting the human sign-off.
    5. On partial or rejection: capture the failed items verbatim and write them into `.planning/phases/04-gpx-route-integration/04-VERIFICATION.md` (one YAML entry per gap with truth/reason/artifacts/missing), then signal the user to run `/gsd:plan-phase 04 --gaps` for a follow-up plan.
  </action>
  <how-to-verify>
    **Setup**
    1. Ensure a real GPX file is available. Recommended: a route from Strava / Komoot / RideWithGPS (clean DEM elevation). Copy the absolute path to clipboard. Example: `/Users/partydj/Downloads/test-route.gpx`.
    2. Power on Wahoo KICKR Core. Make sure no other app (Zwift, Wahoo companion) is connected to it — only RideOS may own the BLE link.

    **Start engine + UI (two terminals)**
    ```bash
    # Terminal 1 — engine
    cd /Users/partydj/Desktop/Projekte/RideOS/engine && uv run python -m engine

    # Terminal 2 — UI
    cd /Users/partydj/Desktop/Projekte/RideOS/ui && npm run dev
    ```
    Open the Vite dev URL (e.g. `http://localhost:5173`) in your browser.

    **Verification checklist — tick each**

    ROUTE-01 (GPX load):
    - [ ] Pre-ride screen appears FIRST on page load (cockpit NOT visible)
    - [ ] File picker accepts only `.gpx` files (selecting a non-gpx shows "Keine .gpx-Datei ausgewählt")
    - [ ] Pasting an absolute GPX path into the text field + clicking "Strecke laden" dismisses the screen; cockpit appears
    - [ ] Within ~1 s the MiniMap fits bounds to the full route (gray Polyline spans the tile) AND the ElevationProfile shows a red→blue gradient area chart (not the flat gray empty-state)
    - [ ] Engine log shows `Route loaded from '...': N points, M.0 m total` (N ≥ 1, M > 0)

    ROUTE-02 (position tracking):
    - [ ] Start pedaling. Within 2 s, the ElevationProfile amber vertical line starts moving rightward
    - [ ] Within 2 s, the MiniMap amber dot starts advancing along the polyline
    - [ ] Faster pedaling ≈ faster marker movement (qualitatively; no stopwatch needed)
    - [ ] Stop pedaling → both markers freeze within 1 tick (~250 ms)

    ROUTE-03 (grade → FTMS):
    - [ ] Resistance changes as you ride. When the amber ElevationProfile marker is in a red (high) part of the gradient, KICKR resistance is high; blue (low) part = low resistance
    - [ ] The `real_grade_pct` number (GradeBar label) updates smoothly; no wild oscillation (±20% spikes indicate Pitfall 2 smoothing missed)

    Edge cases:
    - [ ] Unplug KICKR mid-ride (or power it off briefly). Amber markers FREEZE but UI does not crash. When KICKR reconnects, markers resume advancing where they left off. ConnectionBanner reflects the state transitions.
    - [ ] Ride to the very end of the route. When `position_m` reaches `total_dist_m`: amber markers stay at the end position; GradeBar `real_grade_pct` shows 0%; trainer goes flat; you can keep pedaling freely. Engine log shows `Route complete at M.0 m; grade -> 0.0%`.

    Negative test:
    - [ ] Restart the UI (reload browser). Pre-ride screen appears again. This time click "Ohne Strecke starten" → cockpit appears directly; ElevationProfile + MiniMap stay in empty state; GradeBar shows 0.0% real grade; KICKR stays flat.
    - [ ] Restart UI. Enter a BOGUS path like `/does/not/exist.gpx` + click "Strecke laden". Screen dismisses; cockpit appears; `route_loaded` stays false; ElevationProfile + MiniMap stay empty. Browser DevTools console shows `[RideOS] route_error: FileNotFoundError: ...`.
  </how-to-verify>
  <verify>
    <automated>cd /Users/partydj/Desktop/Projekte/RideOS/engine && uv run pytest -x -q 2>&1 | tail -3 | grep -E "passed" | grep -v failed</automated>
    Manual: user ticks every item in the how-to-verify checklist before signaling "approved".
  </verify>
  <acceptance_criteria>
    - Full engine test suite passes (`cd engine && uv run pytest -x -q` exits 0)
    - UI production build succeeds (`cd ui && npm run build` exits 0)
    - ALL 14 ticks in the how-to-verify checklist are green (user explicitly confirms each)
    - OR user identifies specific failures with enough detail to create a 04-06 gap-closure plan (log lines, screenshots, reproduction steps) — in which case this task writes the VERIFICATION.md gap entries instead of signing off.
  </acceptance_criteria>
  <done>
    Phase 4 acceptance: user ticks every box. If gaps exist, `/gsd:plan-phase 04 --gaps` creates a 04-06 plan from VERIFICATION output.
  </done>
  <resume-signal>Type "approved" if every box ticks. Otherwise paste the list of items that failed, with logs/screenshots.</resume-signal>
</task>

</tasks>

<verification>
- Engine full test suite green
- UI production build succeeds
- Human checklist (14 items across ROUTE-01, ROUTE-02, ROUTE-03, edge cases, negative tests) all ticked
</verification>

<success_criteria>
- Phase 4 success criteria from ROADMAP.md §"Phase 4" all ticked:
  1. GPX loads; elevation profile + coordinates extracted without error
  2. Position advances along route by integrating speed × time (haversine)
  3. Smoothed grade at current position fed into FTMS control loop
  4. Full route ride completes end-to-end with matching resistance
</success_criteria>

<output>
After verification, update `.planning/STATE.md`:
- Mark Phase 4 complete
- Record any gap items discovered
- Next action: Phase 5 — Zwift Click Integration (research spike first, per ROADMAP design notes)
</output>
